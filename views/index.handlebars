<!-- Nav Bar -->
<nav class="navbar navbar-expand-lg navbar-light bg-light">
  <a class="navbar-brand" href="#">Mongo Scraper</a>
  <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
    <span class="navbar-toggler-icon"></span>
  </button>
  <div class="collapse navbar-collapse" id="navbarNav">
    <ul class="navbar-nav">
      <li class="nav-item active">
        <a class="nav-link" href="/">Home</a>
      </li>
      <li class="nav-item">
        <a class="nav-link" href="#">Saved Articles</a>
      </li>
      <li class="nav-item">
        <a class="nav-link" href="#">SCRAPE NEW ARTICLES!</a>
      </li>
    </ul>
  </div>
</nav>

<header>
    <div class="container">
        <div class="row">
            <div class="col-lg-10 mx-auto">
                <h1><strong>Mongo Scraper</strong></h1>
                <h5>New York Times Edition</h5>
            </div>
        </div>
    </div>

</header>

<div class="panel panel-primary">
    {{#each article}}
        <div class= 

</div>

<!-- Table -->
<table class="table table-hover">
    <thead>
        <tr>
            <th scope="col"><a href='{{this.link}}'>Articles</a></th>
            <th scope="col">Summary</th>
        </tr>  
    </thead>
    <tbody>
        {{#each data}}
            <tr><td> {{this.title}}</td><td>{{this.summary}}</a></td><td><button class="btn btn-danger save" data="{{this._id}}" data-state="true">Save Article</button></td></tr>
        {{/each}}
    </tbody>
</table>